#pragma once


namespace ph::network::OAuth
{
    /**
    four roles:
    • Resource owner (the user)
    • Resource server (the API)
    • Authorization server (can be the same server as the API)
    • Client (the third-party app)
     */
}
